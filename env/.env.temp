# ── LLM endpoint (used for description generation and SQL generation) ────────
LLM_BASE_URL = ""
LLM_MODEL    = ""

# ── Target database (the DB being indexed) ───────────────────────────────────
DATABASE_IP = ""
DATABASE_PORT = ""

POSTGRES_USER = ""
POSTGRES_DB_NAME = ""
POSTGRES_PASSWORD = ""

# ── Metadata database (nl2sql_metadata) ──────────────────────────────────────
METADATA_DB_HOST = ""
METADATA_DB_PORT = "5432"
METADATA_DB_USER = "nl2sql_user"
METADATA_DB_NAME = "nl2sql_metadata"
METADATA_DB_PASSWORD = ""

# ── Registered DB credentials (used by api.py for multi-DB execution) ─────────
# JSON map: db_name → connection details. Must match db_name in registered_databases table.
# Add one entry per registered database.
REGISTERED_DB_CREDENTIALS={"your_db_name": {"host": "", "port": 5432, "user": "", "password": ""}}
